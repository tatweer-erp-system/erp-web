// Re-exported from AppSettingsContext — kept for backward compatibility
export { useThemeSystem, AppSettingsProvider as ThemeSystemProvider } from "./AppSettingsContext";
export type { ColorScheme as ThemeName, ThemeMode, ThemeColors } from "./AppSettingsContext";
