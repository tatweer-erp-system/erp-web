# Fix lint and type errors

Fix all linting, formatting, and type issues.

## Steps

1. Run `pnpm lint` to check for ESLint errors
2. Run `pnpm check` to check for TypeScript errors
3. Read each file with errors and apply fixes
4. Re-run both checks to confirm everything passes
5. Report a summary of what was fixed
