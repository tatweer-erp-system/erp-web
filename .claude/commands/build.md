# Production build

Build the project for production and report any errors.

## Steps

1. Run `pnpm build` in the erp-web directory
2. If the build fails, analyze the errors
3. Fix any TypeScript or bundling errors
4. Re-run the build to confirm success
5. Report the build output size
